@model List<Station>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Stations List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Station" asp-action="Create" class="btn btn-primary"><i class="far fa-plus-square"></i>&nbsp; Create New</a>
        </div>
    </div>
    <table class="table table-sm table-striped mt-4">
        <thead class="bg-info">
            <tr>
                <th>
                    Station
                </th>
                <th>
                    City
                </th>
                <th>
                    Capacity
                </th>
                <th>
                    Trains Stationed
                </th>
                <th>
                    Available Slots
                </th>
                <th>
                    Edit/Delete
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td>
                        @obj.Name
                    </td>
                    <td>
                        @obj.City
                    </td>
                    <td>
                        @obj.Capacity
                    </td>
                    <td>
                        @obj.TrainsStationed
                    </td>
                    <td>
                        @obj.AvailableSlots
                    </td>
                    <td>
                        <div class="w-100 btn-group" role="group">
                            <a asp-controller="Station" asp-action="Update" asp-route-id="@obj.StationId" class="btn btn-sm btn-primary"><i class="fas fa-edit"></i></a>
                            <a asp-controller="Station" asp-action="Delete" asp-route-id="@obj.StationId" class="btn btn-sm btn-danger"><i class="fas fa-trash-alt"></i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>